"""openstack_bi — multi-region OpenStack operations/BI reporting suite.

This package is the foundation that `core.py`, `qemu_lifetime_report.py`, and
`web.py` build on. Phase 1 introduces the package and wires the existing
QEMU-lifetime report up to multi-region config; later phases layer a report-
plugin registry and new reports on top.
"""
